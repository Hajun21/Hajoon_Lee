#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void printbin(int val, int bites)
{
	for (int i = bites - 1; i >= 0; --i) {

		printf("%d", val >> i & 1);
	}
	printf("\n");
}

int main()
{
	for (int val = 0; val < 256; val++)
	{
		printf("%3d=", val);
		printbin(val, 8);
	}
	return 0;
}
